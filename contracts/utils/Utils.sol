// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.0;

library Utils {
    //    function compareStringHashes(
    //        string memory hash1, string memory hash2
    //    ) external pure returns (bool) {
    //         return (keccak256(bytes(hash1)) == keccak256(bytes(hash2)));
    //    }
}
